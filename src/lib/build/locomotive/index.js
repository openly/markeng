// Generated by CoffeeScript 1.6.3
var FSManager, LocomotiveThemeBuilder, MarkupBuilder, pageBuilder, path, resourceBuilder, _ref,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

path = require('path');

FSManager = require('../../fs_manager');

pageBuilder = require('./page');

MarkupBuilder = require('../static');

resourceBuilder = require('../static/resource');

LocomotiveThemeBuilder = (function(_super) {
  __extends(LocomotiveThemeBuilder, _super);

  function LocomotiveThemeBuilder() {
    _ref = LocomotiveThemeBuilder.__super__.constructor.apply(this, arguments);
    return _ref;
  }

  LocomotiveThemeBuilder.prototype.createBuildDir = function(opDir) {
    FSManager.rmrf(opDir, '/locomotive/');
    FSManager.createRecursiveDirs(opDir, '/locomotive/public');
    FSManager.createRecursiveDirs(opDir, '/locomotive/app/views');
    return this.buildDir = path.normalize(opDir + '/locomotive/public');
  };

  LocomotiveThemeBuilder.prototype.buildAssets = function() {
    return resourceBuilder.build(this.buildDir, this.buildStaticVersion, {
      css: "stylesheets",
      js: "javascripts"
    });
  };

  LocomotiveThemeBuilder.prototype.buildPages = function(assets) {
    return pageBuilder.build(assets, this.buildDir + '/../app/views');
  };

  return LocomotiveThemeBuilder;

})(MarkupBuilder);

module.exports = LocomotiveThemeBuilder;
